#include "hdf_parser.h"
#include <stdio.h>
#include <stdlib.h>

void parseHdf(csc_string string,HdfPool* pool){
  printf("parse hdf string of length %d",string.length);
  printf("parse content of %s",string.buffer);
}
